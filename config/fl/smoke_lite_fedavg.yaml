model:
  name: efficientnet_b0
  output_dim: 9

train:
  strategy: fedavg
  rounds: 2                     # smoke: just verify round transitions
  local_epochs: 1
  batch_size: 16                # 8GB RAM Mac variant
  lr: 0.001
  optimizer: adam

fl:
  fraction_fit: 1.0
  min_fit_clients: 2
  min_available_clients: 2

dataset:
  name: pathmnist
  root: data/medmnist
  size: 28
  split_path: data/split/smoke_lite.json

model:
  name: efficientnet_b0
  output_dim: 9

train:
  strategy: fedprox
  mu: 0.02                      # exercises the proximal term injection
  rounds: 2
  local_epochs: 1
  batch_size: 64
  lr: 0.001
  optimizer: adam

fl:
  fraction_fit: 1.0
  min_fit_clients: 5
  min_available_clients: 5

dataset:
  name: pathmnist
  root: data/medmnist
  size: 28
  split_path: data/split/smoke.json
